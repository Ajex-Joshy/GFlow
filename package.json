{
  "name": "gflow",
  "version": "1.0.0",
  "description": "GFlow - High-velocity pull request tracker & review board for GitHub",
  "scripts": {
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "build:client": "npm run build --prefix client",
    "start": "node server/src/server.js",
    "test:server": "node --check server/src/app.js && node --check server/src/server.js && node --check api/index.js",
    "test": "npm run test:server && npm run build",
    "build": "npm run build --prefix client"
  },
  "keywords": [
    "github",
    "pull-requests",
    "code-review",
    "developer-tools",
    "triage",
    "primer"
  ],
  "author": "Ajex Joshy",
  "license": "MIT",
  "workspaces": [
    "client",
    "server"
  ]
}
